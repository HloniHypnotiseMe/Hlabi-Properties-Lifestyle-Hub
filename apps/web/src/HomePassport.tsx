import { useEffect, useState } from 'react';

const sections=['PROPERTY','OWNERSHIP','MAINTENANCE','RENOVATIONS','APPLIANCES','INSURANCE','FINANCE','SERVICES','DOCUMENTS','WARRANTIES','PROJECTS','PREFERENCES'] as const;
export default function HomePassport(){const [passport,setPassport]=useState<any>(null);const [section,setSection]=useState<typeof sections[number]>('PROPERTY');const [value,setValue]=useState('{}');const [error,setError]=useState('');
useEffect(()=>{fetch('/api/v1/homeowner/properties/demo-property-1/passport').then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.error??'Unable to load passport');setPassport(d)}).catch(e=>setError(e.message));},[]);
async function save(){setError('');try{const parsed=JSON.parse(value);const r=await fetch('/api/v1/homeowner/properties/demo-property-1/passport',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({section,value:parsed})});const d=await r.json();if(!r.ok)throw new Error(d.error??'Unable to save');setPassport(d);}catch(e){setError(e instanceof Error?e.message:'Invalid passport data');}}
return <section className="shell" id="home-passport" style={{padding:'72px 0'}}><p className="eyebrow">HOME PASSPORT</p><h2>Your property, remembered.</h2><p>One living record for ownership, maintenance, upgrades, documents, warranties, services and future plans.</p>{error&&<p role="alert">{error}</p>}{passport&&<div className="ecosystem-grid" style={{marginTop:24}}>{sections.map(s=><article className="journey" key={s}><h3>{s}</h3><p>{passport.data?.[s]?'Updated':'Not captured yet'}</p><button onClick={()=>{setSection(s);setValue(JSON.stringify(passport.data?.[s]??{},null,2));}}>Open</button></article>)}</div>}<div style={{marginTop:24,maxWidth:720}}><label>Section: <select value={section} onChange={e=>setSection(e.target.value as any)}>{sections.map(s=><option key={s}>{s}</option>)}</select></label><textarea value={value} onChange={e=>setValue(e.target.value)} rows={8} style={{display:'block',width:'100%',marginTop:12}}/><button className="primary" onClick={save} style={{marginTop:12}}>Save to Home Passport</button></div></section>}
