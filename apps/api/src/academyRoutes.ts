import { z } from 'zod';
import type { Express, RequestHandler } from 'express';
import { authenticatedPrincipal } from './authentication.js';
import type { AcademyRepository } from './academyRepository.js';
import type { AcademyAssessmentRepository } from './academyAssessmentRepository.js';
export function registerAcademyRoutes(app:Express,authenticated:RequestHandler,repo:AcademyRepository,assessmentRepo:AcademyAssessmentRepository){const enrollment=z.object({courseId:z.string().min(1)});const progress=z.object({moduleIndex:z.number().int().min(0),eventType:z.string().min(2).max(80),payload:z.record(z.unknown()).default({})});app.get('/api/v1/academy/courses',authenticated,async(_req,res)=>res.json(await repo.listCourses()));app.post('/api/v1/academy/enrollments',authenticated,async(req,res)=>{const p=authenticatedPrincipal(res);const v=enrollment.safeParse(req.body);if(!v.success)return res.status(400).json({error:'INVALID_ENROLLMENT',details:v.error.flatten()});if(!(await repo.getCourse(v.data.courseId)))return res.status(404).json({error:'COURSE_NOT_FOUND'});return res.status(201).json(await repo.enroll({userId:p.userId,courseId:v.data.courseId}));});app.get('/api/v1/academy/enrollments',authenticated,async(_req,res)=>{const p=authenticatedPrincipal(res);return res.json(await repo.listEnrollments(p.userId));});app.get('/api/v1/academy/enrollments/:enrollmentId',authenticated,async(req,res)=>{const p=authenticatedPrincipal(res);const e=await repo.getEnrollment(req.params.enrollmentId,p.userId);if(!e)return res.status(404).json({error:'ENROLLMENT_NOT_FOUND'});return res.json(e);});app.post('/api/v1/academy/enrollments/:enrollmentId/progress',authenticated,async(req,res)=>{const p=authenticatedPrincipal(res);const e=await repo.getEnrollment(req.params.enrollmentId,p.userId);if(!e)return res.status(404).json({error:'ENROLLMENT_NOT_FOUND'});const v=progress.safeParse(req.body);if(!v.success)return res.status(400).json({error:'INVALID_PROGRESS',details:v.error.flatten()});const course=await repo.getCourse(e.courseId);if(!course||v.data.moduleIndex>=course.modules.length)return res.status(400).json({error:'MODULE_NOT_FOUND'});if(v.data.moduleIndex>e.completedModules)return res.status(409).json({error:'SEQUENTIAL_PROGRESS_REQUIRED',nextModuleIndex:e.completedModules});if(v.data.moduleIndex<e.completedModules)return res.status(409).json({error:'MODULE_ALREADY_COMPLETED',completedModules:e.completedModules});const moduleAssessments=await assessmentRepo.listAssessments(e.courseId,v.data.moduleIndex);if(moduleAssessments.length>0){const attempts=await assessmentRepo.listAttempts(e.id,p.userId);const passed=moduleAssessments.some(a=>attempts.some(x=>x.assessmentId===a.id&&x.status==='PASSED'));if(!passed)return res.status(409).json({error:'ASSESSMENT_PASS_REQUIRED',moduleIndex:v.data.moduleIndex,message:'Pass the current lesson assessment before unlocking the next lesson.'});}const event=await repo.recordProgress({enrollmentId:e.id,userId:p.userId,...v.data});return res.status(201).json({event,enrollment:await repo.getEnrollment(e.id,p.userId)});});}
