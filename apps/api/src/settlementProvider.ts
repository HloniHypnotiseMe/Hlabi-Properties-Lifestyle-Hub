import type {SupplierPayoutProvider} from './settlementDomain.js';
export class ConfiguredSupplierPayoutProvider implements SupplierPayoutProvider { readonly name:string; constructor(name=process.env.PAYOUT_PROVIDER??'sandbox'){this.name=name;} async initiatePayout(input:{settlementId:string;supplierId:string;amountMinor:number;currency:string;reference:string;idempotencyKey?:string}){if(this.name.toLowerCase()==='sandbox'&&process.env.PAYOUTS_ENABLED==='true')return {status:'PAID' as const,payoutReference:`sandbox-payout-${input.settlementId}`};if(this.name.toLowerCase()==='sandbox')return {status:'PROCESSING' as const};throw new Error('PAYOUT_PROVIDER_NOT_IMPLEMENTED');}}
