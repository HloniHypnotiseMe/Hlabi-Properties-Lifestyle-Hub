# Hlabi API

Express/TypeScript service boundary for authenticated Lifestyle Hub workflows.

The current repository adapter is intentionally in-memory. Do not connect production homeowner data until verified authentication, server-side authorization, PostgreSQL persistence, audit logging and POPIA controls are implemented.
